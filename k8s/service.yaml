apiVersion: v1
kind: Service
metadata:
  name: job-tracker-api-service
  namespace: job-tracker
spec:
  type: ClusterIP
  selector:
    app: job-tracker-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
